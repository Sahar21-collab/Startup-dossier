export const metadata = {
  title: "Tech News",
  description: "Recent startup and technology news, summarized in plain language.",
};

export default function NewsLayout({ children }) {
  return children;
}
